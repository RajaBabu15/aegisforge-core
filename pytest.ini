[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: needs Postgres
    slow: needs fastembed/sentence-transformers (pip install -r requirements-eval.txt)
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
